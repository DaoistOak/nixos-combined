diff --git a/src/shell/bar/bar.cpp b/src/shell/bar/bar.cpp
index 104ba50..ff9f0d3 100644
--- a/src/shell/bar/bar.cpp
+++ b/src/shell/bar/bar.cpp
@@ -914,43 +914,31 @@ namespace {
         barInnerSurfaceExtension(instance.barConfig, shadowConfig, surfaceWidth, surfaceHeight);
     const auto barVisual =
         computeBarVisualGeometry(instance.barConfig, shadowConfig, surfaceWidth, surfaceHeight, innerSurfaceExtension);
-    // Shadow follows the same shape as the background: the body expanded outward by
-    // the concave inset into the visual rect, so concave spikes cast a matching shadow.
     const float barAreaW = barVisual.width + concave.logicalInset.left + concave.logicalInset.right;
     const float barAreaH = barVisual.height + concave.logicalInset.top + concave.logicalInset.bottom;
     const float bgOpacity = std::clamp(instance.barConfig.backgroundOpacity, 0.0F, 1.0F);
     const auto shadowOff = shadowConfig.shadowOffset();
-    const float shadowX = barVisual.x - concave.logicalInset.left + static_cast<float>(shadowOff.x);
-    const float shadowY = barVisual.y - concave.logicalInset.top + static_cast<float>(shadowOff.y);
-    RoundedRectStyle shadowStyle = shell::surface_shadow::style(
-        shadowConfig, bgOpacity,
-        shell::surface_shadow::Shape{
-            .corners = concave.corners, .logicalInset = concave.logicalInset, .radius = concave.radii
-        }
-    );
-
-    const bool panelShadowExclusion = instance.attachedPanelGeometry.has_value()
-        && instance.attachedPanelGeometry->width > 0.0F
-        && instance.attachedPanelGeometry->height > 0.0F;
-    if (panelShadowExclusion) {
-      const auto& attached = *instance.attachedPanelGeometry;
-      const float convexRadius = std::max(0.0F, attached.cornerRadius);
-      const float bulgeRadius = std::max(0.0F, attached.bulgeRadius);
-      const std::string_view barPosition = instance.barConfig.position;
-      const auto corners = attached_panel::cornerShapes(barPosition);
-      const auto pickRadius = [&](CornerShape shape) {
-        return shape == CornerShape::Concave ? bulgeRadius : convexRadius;
-      };
-      shadowStyle.shadowExclusion = true;
-      shadowStyle.shadowExclusionOffsetX = shadowX - attached.x;
-      shadowStyle.shadowExclusionOffsetY = shadowY - attached.y;
-      shadowStyle.shadowExclusionWidth = attached.width;
-      shadowStyle.shadowExclusionHeight = attached.height;
-      shadowStyle.shadowExclusionCorners = corners;
-      shadowStyle.shadowExclusionLogicalInset = attached_panel::logicalInset(barPosition, bulgeRadius);
-      shadowStyle.shadowExclusionRadius =
-          Radii{pickRadius(corners.tl), pickRadius(corners.tr), pickRadius(corners.br), pickRadius(corners.bl)};
-    }
+    // Continuous 2D shadow object behind the bar: a sharp rectangle covering the
+    // whole bar footprint and extended by the shadow offset on the shadow side,
+    // instead of the curved rounded-edge-only model panels and popups use. The bar
+    // body renders on top, so only the part that hangs outside the bar along the
+    // shadow direction stays visible.
+    const float shadowX = barVisual.x - concave.logicalInset.left;
+    const float shadowY = barVisual.y - concave.logicalInset.top;
+    const float shadowW = std::max(barAreaW, barAreaW + static_cast<float>(shadowOff.x));
+    const float shadowH = std::max(barAreaH, barAreaH + static_cast<float>(shadowOff.y));
+    const float shadowAlpha = std::clamp(shadowConfig.alpha, 0.0F, 1.0F) * bgOpacity;
+    const RoundedRectStyle shadowStyle{
+        .fill = rgba(0.0F, 0.0F, 0.0F, shadowAlpha),
+        .border = Color{},
+        .fillMode = FillMode::Solid,
+        .corners = CornerShapes{},
+        .logicalInset = RectInsets{},
+        .radius = 0.0F,
+        .softness = 0.0F,
+        .borderWidth = 0.0F,
+        .outerShadow = false,
+    };
 
     auto configureShadow = [&](Box* node, float x, float y) {
       if (node == nullptr) {
@@ -959,7 +947,7 @@ namespace {
       node->setStyle(shadowStyle);
       node->setZIndex(-1);
       node->setPosition(x, y);
-      node->setSize(barAreaW, barAreaH);
+      node->setSize(shadowW, shadowH);
     };
 
     instance.shadow->setHitTestVisible(false);
diff --git a/src/shell/screen_corners/screen_corners.cpp b/src/shell/screen_corners/screen_corners.cpp
index 7061417..182063a 100644
--- a/src/shell/screen_corners/screen_corners.cpp
+++ b/src/shell/screen_corners/screen_corners.cpp
@@ -3,6 +3,7 @@
 #include "config/config_service.h"
 #include "core/ui_phase.h"
 #include "ui/controls/screen_corner.h"
+#include "ui/palette.h"
 #include "wayland/wayland_connection.h"
 
 #include <algorithm>
@@ -45,6 +46,20 @@ void ScreenCorners::initialize(WaylandConnection& wayland, ConfigService* config
   m_wayland = &wayland;
   m_config = config;
   m_renderContext = renderContext;
+
+  // Corners blend with the theme background, so follow the active palette.
+  m_paletteConn = paletteChanged().connect([this] {
+    for (auto& inst : m_instances) {
+      for (auto& corner : inst->corners) {
+        if (corner.sceneRoot != nullptr) {
+          static_cast<ScreenCorner*>(corner.sceneRoot.get())->setColor(colorForRole(ColorRole::Surface));
+        }
+        if (corner.surface != nullptr) {
+          corner.surface->requestRedraw();
+        }
+      }
+    }
+  });
 }
 
 void ScreenCorners::onConfigReload() {
@@ -182,7 +197,7 @@ void ScreenCorners::buildCornerScene(Corner& corner, std::uint32_t width, std::u
 
   auto root = std::make_unique<ScreenCorner>();
   root->setSize(logicalWidth, logicalHeight);
-  root->setColor(Color{0.0F, 0.0F, 0.0F, 1.0F});
+  root->setColor(colorForRole(ColorRole::Surface));
   root->setCorner(cornerPosition(cornerIndex));
   root->setExponent(4.0F);
 
diff --git a/src/shell/screen_corners/screen_corners.h b/src/shell/screen_corners/screen_corners.h
index 80641b7..d3a46de 100644
--- a/src/shell/screen_corners/screen_corners.h
+++ b/src/shell/screen_corners/screen_corners.h
@@ -1,5 +1,6 @@
 #pragma once
 
+#include "ui/signal.h"
 #include "wayland/layer_surface.h"
 
 #include <cstdint>
@@ -45,5 +46,6 @@ private:
   RenderContext* m_renderContext = nullptr;
   bool m_lastEnabled = false;
   std::int32_t m_lastSize = 0;
+  Signal<>::ScopedConnection m_paletteConn;
   std::vector<std::unique_ptr<OutputInstance>> m_instances;
 };
